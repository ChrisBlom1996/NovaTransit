# NovaTransit

Real-time logistics dashboard for route managers — mobile-first portfolio project.

## Stack

- Vite + React + TypeScript
- Tailwind CSS (colors via CSS variables)
- Zustand, Framer Motion, Leaflet / react-leaflet, clsx

## Scripts

```bash
npm install
npm run dev
npm run build
```
